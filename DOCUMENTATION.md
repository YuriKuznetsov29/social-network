# Документация проекта Social Network

## 📋 Содержание

1. [Описание проекта](#описание-проекта)
2. [Технологический стек](#технологический-стек)
3. [Структура проекта](#структура-проекта)
4. [Установка и запуск](#установка-и-запуск)
5. [Архитектура](#архитектура)
6. [API документация](#api-документация)
7. [Сущности (Entities)](#сущности-entities)
8. [Фичи (Features)](#фичи-features)
9. [Страницы (Pages)](#страницы-pages)
10. [Тестирование](#тестирование)
11. [CI/CD](#cicd)
12. [Развертывание](#развертывание)

---

## Описание проекта

Social Network — это полнофункциональное веб-приложение социальной сети, разработанное с использованием современного стека технологий. Проект реализует основные функции социальных сетей: регистрацию и аутентификацию пользователей, создание и управление постами, обмен сообщениями в реальном времени, систему друзей, комментарии и лайки.

### Основной функционал:

- ✅ Регистрация и аутентификация пользователей (JWT)
- ✅ Профиль пользователя с возможностью редактирования
- ✅ Создание и публикация постов с изображениями
- ✅ Система комментариев и лайков
- ✅ Лента новостей
- ✅ Система друзей и запросов на добавление в друзья
- ✅ Мессенджер в реальном времени (WebSocket)
- ✅ Загрузка и управление аватарами
- ✅ Поиск пользователей
- ✅ Уведомления
- ✅ Интернационализация (i18n) - русский и английский языки
- ✅ Темная и светлая темы оформления

---

## Технологический стек

### Frontend

- **React 18.2.0** - UI библиотека
- **TypeScript 5.1.6** - типизация
- **Redux Toolkit 1.9.5** - управление состоянием
- **React Router 6.14.2** - маршрутизация
- **Material-UI 5.16.7** - UI компоненты
- **Axios 1.5.0** - HTTP клиент
- **Socket.io-client 4.7.2** - WebSocket клиент для real-time коммуникации
- **i18next 23.5.1** - интернационализация
- **Formik 2.4.3 + Yup 1.2.0** - работа с формами и валидация
- **React Hot Toast 2.5.2** - уведомления
- **Webpack 5.88.2 / Vite 5.4.0** - сборщики
- **SCSS** - стилизация
- **Jest 29.7.0** - unit тестирование
- **Cypress 13.13.2** - E2E тестирование

### Backend

- **Node.js** - серверная платформа
- **Express 4.18.2** - веб-фреймворк
- **MongoDB + Mongoose 7.4.3** - база данных
- **Socket.io 4.7.2** - WebSocket сервер для real-time коммуникации
- **JWT (jsonwebtoken 9.0.1)** - аутентификация
- **bcryptjs 2.4.3** - хеширование паролей
- **express-validator 7.0.1** - валидация данных
- **express-fileupload 1.4.0** - загрузка файлов
- **cookie-parser 1.4.6** - работа с cookies

### Инструменты разработки

- **ESLint** - линтинг кода
- **Prettier** - форматирование кода
- **TypeScript** - типизация
- **Husky** - Git hooks

---

## Структура проекта

```
social-network/
├── client/                 # Frontend приложение
│   ├── src/
│   │   ├── app/           # Инициализация приложения, провайдеры, роутинг
│   │   ├── pages/         # Страницы приложения
│   │   ├── widgets/       # Крупные составные блоки UI
│   │   ├── features/      # Бизнес-логика и функциональность
│   │   ├── entities/      # Бизнес-сущности
│   │   ├── shared/        # Переиспользуемые модули
│   │   └── index.tsx      # Точка входа
│   ├── public/            # Статические файлы
│   ├── config/           # Конфигурация (webpack, jest)
│   ├── cypress/          # E2E тесты
│   └── package.json
│
├── server/                # Backend приложение
│   ├── models/           # Mongoose модели
│   ├── routes/           # API маршруты
│   ├── services/         # Бизнес-логика
│   ├── middleware/       # Express middleware
│   ├── socket/           # WebSocket обработчики
│   ├── config/           # Конфигурация
│   ├── static/           # Статические файлы
│   ├── startUp/          # Инициализация БД
│   └── app.js            # Точка входа
│
└── README.md             # Основная документация
```

---

## Установка и запуск

### Предварительные требования

- Node.js 18 или выше
- MongoDB (локально или MongoDB Atlas)
- npm или yarn

### Установка зависимостей

```bash
# Установка зависимостей для клиента и сервера
npm install

# Или отдельно:
cd client && npm install
cd ../server && npm install
```

### Настройка окружения

1. **Backend конфигурация** (`server/config/default.json`):

```json
{
  "port": 8082,
  "mongoUri": "mongodb://127.0.0.1:27017/app",
  "accessSecret": "your-secret-key",
  "refreshSecret": "your-refresh-secret-key",
  "clientURL": "http://localhost:3000"
}
```

2. **Frontend конфигурация**: Настройки API endpoint находятся в `client/src/shared/api/http/index.ts`

### Запуск в режиме разработки

```bash
# Запуск frontend (порт 3000)
npm start
# или с Vite
npm run start:vite

# Запуск backend (порт 8082)
npm run serve
```

### Скрипты

#### Frontend скрипты

- `npm start` - запуск frontend проекта в dev режиме (Webpack)
- `npm run start:vite` - запуск frontend проекта в dev режиме на Vite
- `npm run build:prod` - сборка в production режиме
- `npm run build:dev` - сборка в dev режиме (не минимизирован)
- `npm run test:unit` - запуск unit тестов
- `npm run test:e2e` - запуск E2E тестов (Cypress)
- `npm run format` - форматирование всего проекта с помощью Prettier
- `npm run lint` - проверка кода с помощью ESLint
- `npm run lint:fix` - автоматическое исправление ошибок ESLint

#### Backend скрипты

- `npm run serve` - запуск backend проекта в dev режиме (с nodemon)
- `npm start` - запуск backend проекта в production режиме

---

## Архитектура

Проект написан в соответствии с методологией **Feature Sliced Design (FSD)**.

### Слои архитектуры:

1. **app** - инициализация приложения, провайдеры, роутинг, глобальные стили
2. **pages** - полноценные страницы приложения
3. **widgets** - крупные самостоятельные блоки интерфейса (Header, SideBar и т.д.)
4. **features** - бизнес-действия пользователя (авторизация, создание поста и т.д.)
5. **entities** - бизнес-сущности (User, Post, Comment и т.д.)
6. **shared** - переиспользуемые модули (UI компоненты, утилиты, конфигурация)

### Принципы FSD:

- ✅ Изоляция слоев - верхние слои могут зависеть от нижних, но не наоборот
- ✅ Публичный API - экспорт только необходимых модулей
- ✅ Абсолютные импорты - использование алиасов (@/shared, @/entities и т.д.)
- ✅ Разделение по смыслу - каждая фича/сущность изолирована

Подробнее: [Feature Sliced Design документация](https://feature-sliced.design/docs/get-started/tutorial)

---

## API документация

### Базовый URL

```
http://localhost:8082/api
```

### Аутентификация

Все защищенные эндпоинты требуют JWT токен в заголовке:
```
Authorization: Bearer <access_token>
```

Refresh token хранится в httpOnly cookie.

#### POST `/api/auth/signUp`

Регистрация нового пользователя.

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "password": "password123",
  "firstName": "Иван",
  "lastName": "Иванов"
}
```

**Ответ (201):**
```json
{
  "accessToken": "jwt_access_token",
  "user": {
    "userId": "user_id",
    "email": "user@example.com",
    "firstName": "Иван",
    "lastName": "Иванов",
    ...
  }
}
```

#### POST `/api/auth/signInWithPassword`

Вход в систему.

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**Ответ (200):**
```json
{
  "accessToken": "jwt_access_token",
  "user": { ... }
}
```

#### GET `/api/auth/token`

Обновление access token с помощью refresh token.

**Ответ (200):**
```json
{
  "accessToken": "new_jwt_access_token",
  "user": { ... }
}
```

### Пользователи

#### GET `/api/user/:userId`

Получение данных пользователя по ID.

**Ответ (200):**
```json
{
  "user": {
    "userId": "user_id",
    "email": "user@example.com",
    "firstName": "Иван",
    "lastName": "Иванов",
    "gender": "male",
    "friends": [],
    "posts": 0,
    "birthDay": "1990-01-01",
    "avatarPath": "/path/to/avatar.jpg",
    "city": "Москва",
    "isOnline": true,
    "lastSeenOnline": "2024-01-01T12:00:00Z"
  }
}
```

#### GET `/api/user/getAllUsers`

Получение списка всех пользователей (требует аутентификации).

#### POST `/api/user/findUser`

Поиск пользователей по имени или email.

**Тело запроса:**
```json
{
  "query": "Иван"
}
```

### Посты

#### GET `/api/post/:userId`

Получение всех постов пользователя.

#### POST `/api/post`

Создание нового поста.

**Тело запроса:**
```json
{
  "text": "Текст поста",
  "imagePath": "/path/to/image.jpg" // опционально
}
```

#### POST `/api/post/:postId/like`

Добавление/удаление лайка к посту.

#### GET `/api/post/:postId/comments`

Получение комментариев к посту.

#### POST `/api/post/:postId/comment`

Добавление комментария к посту.

**Тело запроса:**
```json
{
  "text": "Текст комментария"
}
```

### Файлы

#### POST `/api/file/avatar`

Загрузка аватара пользователя.

**Формат:** `multipart/form-data`

**Поля:**
- `avatar`: файл изображения

**Ответ (200):**
```json
{
  "avatarPath": "/path/to/uploaded/avatar.jpg"
}
```

### Мессенджер

#### GET `/api/messenger/:userId/conversations`

Получение всех бесед пользователя.

#### GET `/api/messenger/:roomId/messages`

Получение сообщений из беседы.

#### POST `/api/messenger/message`

Отправка сообщения (также через WebSocket).

---

## Сущности (Entities)

Сущности представляют бизнес-объекты приложения:

### Avatar
Компонент для отображения аватара пользователя.

**Путь:** `client/src/entities/Avatar`

### Comment
Компоненты для работы с комментариями к постам.

**Путь:** `client/src/entities/Comment`

### ConversationLink
Ссылки на беседы в мессенджере.

**Путь:** `client/src/entities/ConversationLink`

### Dialog
Компоненты диалогов в мессенджере.

**Путь:** `client/src/entities/Dialog`

### Friends
Компоненты для работы с друзьями пользователя.

**Путь:** `client/src/entities/Friends`

### Message
Компоненты сообщений в мессенджере.

**Путь:** `client/src/entities/Message`

### News
Логика и компоненты ленты новостей.

**Путь:** `client/src/entities/News`

### Post
Компоненты постов пользователя.

**Путь:** `client/src/entities/Post`

### SettingBtn
Кнопки настроек.

**Путь:** `client/src/entities/SettingBtn`

### UserData
Работа с данными пользователя (Redux slice, селекторы, сервисы).

**Путь:** `client/src/entities/UserData`

### WhoLikesIt
Компоненты для отображения пользователей, лайкнувших пост.

**Путь:** `client/src/entities/WhoLikesIt`

---

## Фичи (Features)

Фичи представляют бизнес-действия пользователя:

### AuthByEmail
Аутентификация по email и паролю.

**Путь:** `client/src/features/AuthByEmail`

**Функции:**
- Регистрация пользователя
- Вход в систему
- Проверка аутентификации
- Выход из системы

### FindUsers
Поиск пользователей по имени или email.

**Путь:** `client/src/features/FindUsers`

### GetAnotherUserData
Получение данных другого пользователя.

**Путь:** `client/src/features/GetAnotherUserData`

### GetFriendsData
Получение данных друзей пользователя.

**Путь:** `client/src/features/GetFriendsData`

### Messenger
Мессенджер с real-time коммуникацией через WebSocket.

**Путь:** `client/src/features/Messenger`

**Функции:**
- Список бесед
- Отправка и получение сообщений в реальном времени
- Создание новых бесед

### Notifications
Система уведомлений.

**Путь:** `client/src/features/Notifications`

### CreatePost (PostHandler)
Создание и управление постами.

**Путь:** `client/src/features/PostHandler`

**Функции:**
- Создание поста с текстом и изображением
- Лайки постов
- Комментарии к постам

### UploadAvatar
Загрузка аватара пользователя.

**Путь:** `client/src/features/UploadAvatar`

---

## Страницы (Pages)

### ProfilePage
Страница профиля пользователя.

**Путь:** `client/src/pages/ProfilePage`

**Функции:**
- Отображение информации о пользователе
- Список постов пользователя
- Редактирование профиля

### AnotherUserPage
Страница просмотра профиля другого пользователя.

**Путь:** `client/src/pages/AnotherUserPage`

### ChangeProfilePage
Страница редактирования профиля.

**Путь:** `client/src/pages/ChangeProfilePage`

### NewsPage
Лента новостей со всеми постами друзей.

**Путь:** `client/src/pages/NewsPage`

### MessengerPage
Страница мессенджера.

**Путь:** `client/src/pages/MessengerPage`

### ConversationPage
Страница отдельной беседы.

**Путь:** `client/src/pages/ConversationPage`

### FriendsPage
Страница со списком друзей и запросами в друзья.

**Путь:** `client/src/pages/FriendsPage`

### NotFoundPage
Страница 404.

**Путь:** `client/src/pages/NotFoundPage`

---

## Тестирование

### Unit тесты

Используется **Jest** + **React Testing Library**.

```bash
npm run test:unit
```

Тесты находятся рядом с тестируемыми компонентами или в папке `__tests__`.

### E2E тесты

Используется **Cypress**.

```bash
# Открыть Cypress UI
npm run test:e2e

# Запустить в headless режиме
npx cypress run
```

Тесты находятся в `client/cypress/e2e/`:
- `routing.cy.ts` - тесты маршрутизации
- `post-create.cy.ts` - тесты создания постов
- `profile-edit.cy.ts` - тесты редактирования профиля

---

## CI/CD

Проект использует GitHub Actions для автоматизации тестирования и развертывания.

### Конфигурация

CI/CD workflow находится в файле `.github/workflows/main.yml`.

### Триггеры

Workflow запускается автоматически при:
- Push в ветку `main`
- Pull Request в ветку `main`

### Процесс CI/CD

#### Job: Tests (Тестирование)

Выполняется на `ubuntu-latest` с Node.js 21.x:

**Шаги:**
1. Checkout кода
2. Настройка Node.js окружения
3. Установка зависимостей (`npm install` в папке `client`)
4. Запуск unit тестов (`npm run test:unit`)

**Особенности:**
- Тесты запускаются даже при ошибках предыдущих шагов (`if: always()`)
- Используется матричная стратегия для поддержки разных версий Node.js

#### Job: Deploy (Развертывание)

Выполняется на `self-hosted` runner:

**Шаги:**
1. **Остановка контейнера** - останавливает существующий Docker контейнер `sn`
2. **Удаление контейнера** - удаляет старый контейнер
3. **Обновление кода** - выполняет `git pull` для получения последних изменений
4. **Сборка образа** - создает новый Docker образ из Dockerfile
5. **Запуск контейнера** - запускает новый контейнер с:
   - Автоматическим перезапуском (`--restart=always`)
   - Именем `sn`
   - Volume для статических файлов (`-v sn_static_data:/app/static`)
   - Маппингом порта 8082 (`-p 8082:8082`)

### Структура workflow

```yaml
name: testing, deployment
on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
jobs:
    tests:
        runs-on: ubuntu-latest
        # ... тестирование
    deploy:
        runs-on: self-hosted
        # ... развертывание
```

### Требования для self-hosted runner

Для работы job `deploy` необходимо:

1. **Self-hosted runner** - настроенный GitHub Actions runner на сервере
2. **Docker** - установленный Docker на сервере
3. **Git** - настроенный доступ к репозиторию
4. **Права доступа** - runner должен иметь права на:
   - Остановку/запуск Docker контейнеров
   - Выполнение `git pull`
   - Сборку Docker образов

### Мониторинг

Статус выполнения CI/CD можно отслеживать:
- На странице Actions в GitHub репозитории
- В уведомлениях GitHub
- Через API GitHub

### Ручной запуск

Workflow запускается автоматически, но также можно запустить вручную через:
- GitHub Actions UI (если настроен `workflow_dispatch`)
- Push/PR в ветку `main`

---

## Развертывание

### Docker

Проект включает Dockerfile для развертывания backend:

```dockerfile
FROM node:18
WORKDIR /app
COPY server/package.json /app
RUN npm install
COPY server /app
COPY client/build /app/client
COPY client/public/locales /app/client/locales
EXPOSE 8082
CMD [ "npm", "start" ]
```

### Production сборка

```bash
# Сборка frontend
cd client
npm run build:prod

# Запуск backend (использует собранный frontend)
cd ../server
npm start
```

### Переменные окружения

Для production настройте:

- `NODE_ENV=production`
- MongoDB URI (через config)
- Секретные ключи для JWT
- CORS настройки
- Статические пути для файлов

---

## Дополнительная информация

### Абсолютные импорты

В проекте используются алиасы для импортов:

```typescript
import { Button } from '@/shared/ui/Button'
import { UserData } from '@/entities/UserData'
```

Настройка в `tsconfig.json`:
```json
{
  "paths": {
    "@/*": ["./src/*"]
  }
}
```

### Интернационализация

Проект поддерживает русский и английский языки. Переводы находятся в:
- `client/public/locales/ru/`
- `client/public/locales/en/`

### Темы оформления

Приложение поддерживает светлую и темную темы. Реализовано через Material-UI ThemeProvider и локальное хранилище.

### WebSocket

Real-time коммуникация реализована через Socket.io. События:
- Подключение/отключение пользователей
- Отправка сообщений
- Статус онлайн/оффлайн

